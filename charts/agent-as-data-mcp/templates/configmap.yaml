apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agent-as-data-mcp.fullname" . }}
  labels:
    app: {{ include "agent-as-data-mcp.name" . }}
data:
  config.yaml: |
    {{- include "agent-as-data-mcp.config" . | nindent 4 }}
