webservice:
  address: "0.0.0.0:8085"
  cors:
    allow_origins: ["*"]
    allow_methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
    allow_headers: ["Content-Type", "Authorization"]

database:
  url:
    url: "postgresql://postgres:postgres@localhost:5432/agent_as_data"
  max_connections: 10
